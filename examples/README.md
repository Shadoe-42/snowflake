# Examples

Status: PLACEHOLDER -- empty until the architecture docs are stable enough to demonstrate
concretely. Phase 1 is deliberately domain-agnostic, so illustrative SQL/objects here should
stay generic rather than assuming a business scenario -- Phase 2 (GenAI) and Phase 3 (fin)
are where concrete domains get picked.
